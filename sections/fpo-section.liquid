<div class="fpo-section-wrapper">
  <section class="fpo-section">
    <!-- Main content with video -->
    <div class="fpo-content">
      <div class="fpo-image-wrapper">
        {% if section.settings.video != blank %}
          <video 
            src="{{ section.settings.video }}"
            class="fpo-image fpo-video"
            muted
            playsinline
            autoplay
          ></video>
        {% endif %}
      </div>
      
      <!-- Added console log for debugging and improved text rendering -->
      <div class="fpo-text-overlay">
        {% if section.blocks.size > 0 %}
          {% for block in section.blocks %}
            {% if block.settings.text_line != blank %}
              <p class="fpo-overlay-text">{{ block.settings.text_line }}</p>
            {% endif %}
          {% endfor %}
        {% else %}
          <!-- Fallback text if no blocks are added -->
          <p class="fpo-overlay-text">Introducing</p>
          <p class="fpo-overlay-text">SWAN in Black and White —</p>
          <p class="fpo-overlay-text">two timeless finishes,</p>
          <p class="fpo-overlay-text">one transformative experience.</p>
          <p class="fpo-overlay-text">Designed for creators.</p>
          <p class="fpo-overlay-text">Built for beauty. Ready for</p>
          <p class="fpo-overlay-text">every partnership ahead.</p>
        {% endif %}
      </div>
    </div>
  </section>
</div>

<style>
  .fpo-section-wrapper {
    width: 100%;
    height: 100dvh;
    padding: 0 24px;
    margin: 24px 0;
  }

  .fpo-section {
    width: 100%;
    height: 100%;
    position: relative;
  }

  .fpo-content {
    width: 100%;
    height: 80vh;
    text-align: center;
    background-color: {{ section.settings.background_color }};
    border-radius: 32px;
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  .fpo-image-wrapper {
    width: 100%;
    height: 100%;
    position: absolute;
    top: 0;
    left: 0;
    z-index: 1;
  }

  .fpo-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* Improved text overlay visibility and contrast */
  .fpo-text-overlay {
    position: relative;
    z-index: 10;
    text-align: center;
    padding: 32px;
    max-width: 90%;
    pointer-events: none;
  }

  .fpo-overlay-text {
    font-size: 7vw;
    font-weight: 100;
    line-height: 1.4;
    margin: 0;
    color: {{ section.settings.overlay_text_color | default: '#ffffff' }};
    opacity: 1;
  }

  @media (min-width: 768px) {
    .fpo-section-wrapper {
      min-height: 50dvh;
      padding-bottom: 24px;
    }

    .fpo-content {
      height: 100%;
    }

    .fpo-text-overlay {
      max-width: 1200px;
      padding: 48px;
    }

    .fpo-overlay-text {
      font-size: 3.5vw;
      font-weight: 100;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const videos = document.querySelectorAll('.fpo-video');
    
    console.log('[v0] FPO section loaded, found videos:', videos.length);
    
    videos.forEach(video => {
      video.loop = false;
      
      video.addEventListener('ended', function() {
        this.pause();
      });
      
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            console.log('[v0] Video is visible, attempting to play');
            video.play().catch(err => {
              console.log('[v0] Video autoplay blocked by browser:', err);
              document.addEventListener('click', () => video.play(), { once: true });
            });
          } else {
            video.pause();
          }
        });
      }, {
        threshold: 0.3
      });
      
      observer.observe(video);
    });
    
    const textOverlay = document.querySelector('.fpo-text-overlay');
    const textLines = document.querySelectorAll('.fpo-overlay-text');
    console.log('[v0] Text overlay found:', !!textOverlay);
    console.log('[v0] Text lines found:', textLines.length);
  });
</script>

{% schema %}
{
  "name": "FPO Section",
  "settings": [
    {
      "type": "url",
      "id": "video",
      "label": "Video URL",
      "info": "Upload video to Files in Shopify admin, then paste the URL here. Video will autoplay when section is visible."
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#ec4899"
    },
    {
      "type": "color",
      "id": "overlay_text_color",
      "label": "Overlay Text Color",
      "default": "#ffffff"
    }
  ],
  "blocks": [
    {
      "type": "text_line",
      "name": "Text Line",
      "settings": [
        {
          "type": "text",
          "id": "text_line",
          "label": "Text Line",
          "default": "Your text here"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "FPO Section",
      "blocks": [
        {
          "type": "text_line",
          "settings": {
            "text_line": "Introducing"
          }
        },
        {
          "type": "text_line",
          "settings": {
            "text_line": "SWAN in Black and White —"
          }
        },
        {
          "type": "text_line",
          "settings": {
            "text_line": "two timeless finishes,"
          }
        },
        {
          "type": "text_line",
          "settings": {
            "text_line": "one transformative experience."
          }
        },
        {
          "type": "text_line",
          "settings": {
            "text_line": "Designed for creators."
          }
        },
        {
          "type": "text_line",
          "settings": {
            "text_line": "Built for beauty. Ready for"
          }
        },
        {
          "type": "text_line",
          "settings": {
            "text_line": "every partnership ahead."
          }
        }
      ]
    }
  ]
}
{% endschema %}
